name: Deploy

on:
  workflow_call:

jobs:
  build:
     runs-on: self-hosted
     steps:

       -name: cloned the code
       uses: action/checkout@v7

       -name: Deploy via nginx
        run: cp index.html /var/www/html
       - name: Deploy the code
         run: echo "Code Deployed to AWS EC2 useing Instance"
